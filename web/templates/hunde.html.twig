{% extends 'html.html.twig' %}

{% import 'products.html.twig' as products %}

{% block title %}Produkte für Hunde | {% endblock %}

{% block content %}

<div class="products">

	<h1>Produkte für Hunde</h1>

	{{ products.set('hunde', 'die_bunten', 'Die Bunten', images) }}
	{{ products.set('hunde', 'die_exclusiven', 'Die Exclusiven', images, 'left') }}
	{{ products.set('hunde', 'die_punzierten', 'Die Punzierten', images) }}

</div>

{% endblock %}
